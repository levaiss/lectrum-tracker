// Core
import { FC } from 'react';

// Components
import { Navigation } from '../Navigation';

export const Header: FC = () => {
    return (
        <header>
            <Navigation />
        </header>
    );
};
